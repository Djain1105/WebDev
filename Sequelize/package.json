{
  "name": "sequelize",
  "version": "1.0.0",
  "description": "Opening MYSQL shell \r type in command prompt : ```mysql -u root -p```   (root is the default user and -p to enter the password in the next step)",
  "main": "connect.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mysql2": "^3.12.0",
    "sequelize": "^6.37.5"
  }
}
